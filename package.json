{
  "name": "tigries-app",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "preview": "npm run preview --workspace frontend",
    "supabase:start": "npm run supabase:start --workspace backend",
    "supabase:stop": "npm run supabase:stop --workspace backend",
    "supabase:status": "npm run supabase:status --workspace backend",
    "supabase:reset": "npm run supabase:reset --workspace backend",
    "seed:remote": "npm run seed:remote --workspace backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
